
# Renderer frontend

The renderer module loads resources from IR
into graphics memory. The idea is to access an
abstraction of the resources from the code,
letting the messy internal state be handled by
the library.

